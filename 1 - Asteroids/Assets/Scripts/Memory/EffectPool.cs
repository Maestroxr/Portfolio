using Gamebox;

namespace Portfolio.Asteroids
{
    /// <summary>Pool of one visual effect (an explosion, a spark, a shockwave...).</summary>
    public class EffectPool : PrefabPool<PooledEffect>
    {
    }
}
